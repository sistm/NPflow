# C++ implementation of residual trace computation step used when sampling the scale

C++ implementation of residual trace computation step used when sampling
the scale

## Usage

``` r
traceEpsC(eps, sigma)
```

## Arguments

- eps:

  a numeric matrix where each column contains the centered and unskewed
  observations

- sigma:

  a numeric covariance matrix

## Value

the computed trace
